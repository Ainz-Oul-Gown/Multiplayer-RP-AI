# Гибридный ИИ-движок для текстовых RPG

## Цель

Проект строится как PWA с frontend на GitHub Pages, backend на Supabase и единым LLM-провайдером OpenRouter / MiMo v2.5.

## Конвейер хода

1. **Парсинг**: действие игрока отправляется в MiMo с системным промптом JSON-контракта.
2. **Математика**: Edge Function валидирует JSON, проверяет предметы, бросает d20 и обновляет PostgreSQL в транзакциях.
3. **Нарратив**: строгие факты, лор и состояние игрока отправляются в MiMo для литературного ответа Мастера.
4. **Трансляция**: итоговая запись попадает в `master_messages` и рассылается через Supabase Realtime.

## Первая итерация

- Минимальный PWA-каркас на Vite + React.
- Контракты промптов и d20-утилиты на frontend для прототипирования.
- Начальная миграция Supabase для миров, лора, сессий, игроков, инвентаря и сообщений Мастера.
- Черновая Edge Function `process-turn` как точка интеграции OpenRouter и Supabase.

## Настройки рантайма

PWA поддерживает локальное хранение настроек подключения:

- `OpenRouter API key` сохраняется в `localStorage` и отправляется в Edge Function заголовком `x-openrouter-api-key`.
- `OpenRouter model` по умолчанию равен `mimo/mimo-v2.5`, но может быть переопределен в интерфейсе.
- `Supabase URL` и `Supabase anon key` можно задать без пересборки приложения; значения из `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY` используются как дефолты.

Для production-сценария предпочтительнее держать OpenRouter key в Supabase Secrets. Клиентский ключ нужен для локальных прототипов и персональных пользовательских ключей.

## Следующие маленькие шаги

1. Добавить полноценную авторизацию Supabase Auth.
2. Подключить Realtime-подписку на `master_messages`.
3. Разнести dashboard, настройки и игровой экран на отдельные компоненты.
4. Ужесточить JSON schema для парсера и заменить fallback на UI-ошибку «перефразируйте действие».

## Игровой экран: текущая способность

После заполнения runtime-настроек экран уже может отправлять реальную заявку игрока:

1. Frontend вызывает Supabase Edge Function `process-turn` через `supabase.functions.invoke`.
2. OpenRouter key и выбранная модель передаются заголовками, поэтому можно тестировать персональные ключи без пересборки PWA.
3. История Мастера загружается из `master_messages`, а новые сообщения приходят через Supabase Realtime.
4. Edge Function подбирает короткие фрагменты лора по тегам цели и использованных предметов, затем добавляет их в промпт рассказчика.

## Безопасность и инвайты

Третья миграция добавляет `session_invites` для приглашений игроков и включает RLS на основные таблицы. Владельцы миров управляют мирами и лором, владельцы сессий меняют настройки комнаты, игроки читают свою партию, инвентарь и сообщения Мастера своей сессии.
